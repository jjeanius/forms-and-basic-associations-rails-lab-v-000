<%= form_for @song do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.text_field :artist_name %>

  <%= f.collection_select :genre_name,  Genre.all, :song_genre_id, :name, {:selected => :genre_name} %>

  <input_field song[:note_contents][] >
  <input_field song[:note_contents][] >

  <%= f.submit "commit" %>
<% end %>
